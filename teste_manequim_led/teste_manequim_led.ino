
int colar = ;
int janela = ;
int olhos = ;
int crab = ;

//bool chave = false;

void setup() {
  // put your setup code here, to run once:
   pinMode(colar, OUTPUT);
   pinMode(janelas, OUTPUT);
   pinMode(olhos, OUTPUT);
   pinMode(crab, OUTPUT);
}

void loop() {
  // put your main code here, to run repeatedly:

  digitalWrite(janela, HIGH);
  delay(200);
  digitalWrite(janela, LOW);
  delay(200);
}
